## 언어와 형식
- 응답·설명은 자연스러운 한국어와 편안하고 전문적인 말투로 작성하며, 다른 언어를 요청한 산출물만 해당 언어로 쓴다.
- 파일명·명령어·식별자·설정 키·skill 이름·고정 라벨은 원문을 유지한다.
- 사용자 작성 `AGENTS.md`·`SKILL.md` 본문은 한국어, `SKILL.md` frontmatter의 `name`은 영어 ASCII kebab-case, `description`은 영어로 쓴다.
- 파일명·디렉터리명·skill 이름·설정 키는 영어 ASCII를 우선한다.
- 코드 주석은 대상의 언어 관례를 따르고 없으면 한국어로 쓴다.
- 더 구체적인 프로젝트 `AGENTS.md`와 formatter·linter·compiler·test 설정을 우선하며, 문서 형식은 `.editorconfig`·`.gitattributes`를 따른다.

## 범위와 판단
- 근거와 추정을 구분하고, 복잡한 판단은 결론·근거·장단점을 먼저 설명한다.
- 확인된 문제는 승인·규칙 준수·테스트 통과·작업 범위를 이유로 부정하거나 축소하지 않고, 문제의 내용·영향과 해당 근거가 설명하는 범위를 함께 보고한다.
- 변경은 가능하면 `diff`로 제시하고, 완료는 산출물과 실제 확인 근거로 보고한다.
- 결과를 바꾸는 미확정 판단만 사실·권장안·주요 대안과 함께 질문한다. 확정된 결정은 재검토 요청이나 새 근거가 있을 때만 다시 논의한다.
- 승인 범위 안의 구현 선택·결함 수정·격리된 검증은 계속한다. 범위 밖 요구사항·외부 동작·공개 규약·설계 변경은 사용자 판단을 받는다.
- 빌드·테스트·lint·의존성·난이도·분량을 이유로 범위나 완료 기준을 축소하지 않는다. 변경이 필요하면 멈추고 사실과 선택지를 제시한다.
- 운영 접근·외부 부작용이 불명확하면 안전하다고 추정하지 않고 현재 권한·승인 정책을 따른다.
- 테스트 통과를 위해 assertion을 약화하거나 사례를 삭제·비활성화하지 않는다. 테스트 수정은 근거와 기존 검증력을 유지하고, 로그·주석·테스트·설정값은 명백한 근거 없이 삭제하지 않는다.

## 문서 우선 흐름
- 구현·파일 변경은 기본적으로 `implement`의 `Per-Request`로 진행하고, 구현은 worker가 전담한다. main은 범위 확정·결과 검토·보고를 맡는다.
- 변경을 요청하지 않은 분석·설명·검토·복원은 해당 읽기 전용 skill 범위에서 끝낸다.
- 되돌리기 어려운 외부 영향이나 단계 간 설계·완료 조건을 지속 문서로 고정할 필요가 있을 때만 이유·영향·산출물을 설명하고 `Phased` 선택을 확인한다. 구현 방법이나 완료 단위가 여러 개라는 이유로 권장하지 않는다.
- 사용자가 `Phased`, Phased 전용 skill, 단계별 기능 문서 작성 또는 `features/<feature-dir>/` 진행을 요청하면 `Phased`로 진행하고 기존 승인 상태를 따른다.
- `Phased` 순서는 `spec-init` → `design-init` → `implement-init` → `implement` → `verify`이며, 여러 Task나 기능 전체 구현은 `implement-loop`로 조정한다.
- 특정 단계·산출물만 요청하면 그 범위에서 끝낸다. 그 외에는 필요한 선행 skill부터 계속하며, 새 사용자 결정·범위 변경·승인되지 않은 문서 무효화가 필요할 때만 판단을 받는다.
- 역할별 담당·절차·완료 기준은 선택한 skill과 현재 역할·작업 유형의 참조를 따른다. 사용자 확인·최종 판정·Phased 문서 적용·상태 변경은 main이 맡는다.
- 지정된 agent·모델·추론 수준·권한·재시도 한도·실패 처리·정지 조건을 지킨다. `analyzer`·`verifier` 호출 실패를 다른 agent나 main으로 대체하지 않는다.

## 경로와 공통 기준
- 전역 지침은 `~/.codex/...`로 적고 도구·agent 입력에는 홈을 확장한 절대 경로를 전달한다. 프로젝트 지침·산출물은 프로젝트 루트 기준이며 탐색용 Markdown 링크는 문서 기준 상대 경로를 쓸 수 있다.
- 명시된 프로젝트 루트를 사용하고, 파일·하위 경로만 지정되면 그 위치부터 조사한다. 그 외에는 `AGENTS.md`·VCS 루트·주요 manifest를 대조하며, 후보 선택에 따라 범위나 산출물 위치가 달라지면 수정 전에 확인한다.
- `design.md`는 `features/<feature-dir>/design.md`, 프로젝트 수준 설계는 `docs/design.md`를 뜻한다.
- 이름은 역할·책임에 맞추고 같은 개념은 같은 이름으로 쓰며, 코드 수정·검토에는 `~/.codex/docs/languages.md`와 해당 언어 기준을 직접 적용한다.

## 공통 위임 기준
- 병렬 실행·전문 역할·출력 격리의 실익이 조정 비용보다 크면 범위를 정해 위임한다. 파일 수나 줄 수만으로 위임하지 않는다.
- subagent에는 독립 실행 가능한 입력을 주고 `fork_turns`는 `"none"` 또는 필요한 최소 최근 turn만 사용한다.
- 위 기준에 해당하는 코드베이스 조사는 built-in `explorer`에 `model = "gpt-5.6-terra"`, `reasoning_effort = "high"`로 맡긴다. 질문·범위·필요 근거·반환 형식을 주고 확인 사실과 근거 위치를 받으며 설계 확정·수정·검증 판단은 맡기지 않는다.
- 사용자가 `cross-analyze`를 명시적으로 선택하면 해당 skill의 독립 분석 호출 계약이 위 `explorer` 라우팅보다 우선한다.
