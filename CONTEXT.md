# Context

저장: 2026-09-02 18:14 +09:00

## 현재 목표

GPT-5.6 Sol의 자동 압축이 지나치게 자주 일어나지 않도록 Codex 컨텍스트 창과 자동 압축 기준을 조정한다. 1M 전체를 쓰는 것보다 약 500K 수준의 연속성을 확보하는 것이 사용자의 우선 목표다.

## 현재 상태

- 조사 완료: GPT-5.6 Sol은 Codex 출시 시 원시 창 `372000`, 유효 창 `353400`이었으나 2026-07-13 전후 원시 창 `272000`, 유효 창 `258400`으로 되돌아갔다. Codex 0.144.6 릴리스에도 세 변형의 창을 `272000`으로 수정한 기록이 있다.
- OpenAI 제품 책임자의 공개 설명은 `372K` 적용 뒤 구독 사용량이 의도보다 많이 차감되어 `272K`로 되돌렸다는 것이다. 별도로 API 과금은 입력이 `272K`를 넘으면 요청 전체에 입력 `2배`, 출력 `1.5배` 요율을 적용한다. 두 사실은 비용 원인과 잘 맞지만, 구독 사용량 산식이 API 요율과 완전히 같다고 확인된 것은 아니다.
- `model_auto_compact_token_limit_scope`는 창 크기가 아니라 자동 압축 임계값에 어떤 토큰을 셀지 정한다. 기본값 `total`은 현재 활성 컨텍스트 전체를 세고, `body_after_prefix`는 압축 뒤 보존된 prefix 이후의 증가분을 세어 즉시 재압축을 줄인다. `body_after_prefix`에서도 유효 컨텍스트 창의 상한은 강제로 지킨다.
- 현재 `config.toml`에는 `model_context_window`와 자동 압축 재정의가 없다. 로컬 세션 기록은 `258400`을 사용했고, 장시간 세션에서 79분 동안 압축이 3회 발생한 사례가 확인됐다.
- 일회성 실행 검증에서 `model_context_window=500000`은 받아들여졌고 유효 창 `475000`으로 보고됐다. 공개 Codex 변경 기록상 GPT-5.6 계열의 설정 재정의 상한은 원시 창 `872000`이며, 95% 정책 적용 시 유효 창은 최대 약 `828400`이다.
- 검증: 로컬 세션 179개와 현재 설정을 읽어 수치를 대조했고, 영구 설정을 바꾸지 않은 app-server 단일 요청으로 `500000 → 475000` 적용을 확인했다.
- Git: `main`, 기준 HEAD `1c308704b122e60d5ea1d8498badf9f2341b7aaf`.

## 현재 작업 문서

없음

## 확정된 결정

- 사용자는 자동 압축 빈도를 낮추는 것을 우선하며, 1M까지는 필요 없고 약 500K면 충분하다고 밝혔다.
- 이번 인수인계 저장 단계에서는 실제 컨텍스트 설정을 변경하지 않는다.

## 미확정 판단

- “500K”를 원시 설정값 `500000`으로 볼지, 화면에 보이는 유효 창 약 `500000`으로 볼지 확정해야 한다. 전자는 유효 `475000`, 후자는 원시값을 약 `526316`으로 설정해야 한다.
- 권장 후보는 원시 창 `500000`, 자동 압축 기준 `420000`, 범위 `body_after_prefix`다. 연속성은 좋아지지만 `272K`를 넘는 긴 요청부터 구독 사용량이 더 빨리 줄 수 있다.

## 다음 작업

- 작업: 원시 `500000`/유효 `475000` 권장안을 사용자와 확정한 뒤 `config.toml`에 컨텍스트 창, 자동 압축 임계값과 범위를 적용한다.
- 완료 기준: 새 Codex 실행이 예상 유효 창을 보고하고 일반 요청이 성공하며, 설정 파일 구문과 실제 적용값이 일치한다.

## 먼저 읽을 파일

- [config.toml](./config.toml)

## 문서 반영 필요

없음
