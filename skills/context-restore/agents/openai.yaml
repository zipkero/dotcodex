interface:
  display_name: "Context Restore"
  short_description: "CONTEXT.md에서 설계 맥락과 다음 작업을 복원"
  default_prompt: "$context-restore를 사용해 CONTEXT.md에서 설계 맥락을 복원하고 다음 작업을 이어가 주세요."

policy:
  allow_implicit_invocation: false
